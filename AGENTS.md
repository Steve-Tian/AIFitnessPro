---

# 项目上下文持续记忆规则

本项目使用 `docs/PROJECT_CONTEXT.md` 作为持续上下文记忆文件。

## 一、每次开始开发前必须执行

Codex 在任何代码修改前，必须先阅读：

1. `docs/PROJECT_CONTEXT.md`
2. 当前项目目录结构
3. 最近相关代码文件
4. `git status`

阅读后，先不要立即修改代码，必须先用简短文字说明：

1. 当前项目目标是什么
2. 当前已经完成了什么
3. 当前开发暂停点在哪里
4. 本次应该继续做什么
5. 本次计划修改哪些文件

## 二、每次阶段性开发结束后必须执行

每完成一个阶段性任务，Codex 必须更新 `docs/PROJECT_CONTEXT.md`。

更新内容包括：

1. 当前项目状态
2. 本轮完成内容
3. 本轮修改文件
4. 新增功能
5. 修复问题
6. 当前未完成事项
7. 已知 bug 或风险
8. 下一步开发任务
9. 下一个 AI 会话应该从哪里继续
10. 建议的 git commit message

## 三、更新原则

1. `PROJECT_CONTEXT.md` 是项目记忆，不是聊天记录。
2. 不要把无关对话、临时废话、重复内容写进去。
3. 内容要短、准、可执行。
4. 保留最新状态，删除已经过期的信息。
5. 每次更新后，要把“当前暂停点”和“下一步任务”写清楚。
6. 不允许在未阅读 `PROJECT_CONTEXT.md` 的情况下继续开发。
7. 如果发现 `PROJECT_CONTEXT.md` 与当前代码不一致，应先根据代码实际情况修正该文件。

## 四、固定结束动作

每次完成开发后，必须输出：

1. 本轮完成了什么
2. 修改了哪些文件
3. 是否已更新 `docs/PROJECT_CONTEXT.md`
4. 当前项目暂停点
5. 下一步建议
6. 建议执行的 git commit 命令
